# Zarr servers

- hosts: "{{ idr_environment_idr }}-dev"

  roles:
    - role: ome.cli_utils

  tasks:
  - name: install Mesa libGL development package
    become: yes
    package:
      name:
        - mesa-libGL-devel
