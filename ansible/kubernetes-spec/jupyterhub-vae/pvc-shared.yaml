# PVC for hub shared scratch space
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jupyterhub-sharedscratch-rw
  namespace: "{{ jupyterhub_vae_namespace }}"
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: gluster
  resources:
    requests:
      storage: 100Mi
